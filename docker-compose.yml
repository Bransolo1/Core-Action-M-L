services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      # Reads ANTHROPIC_API_KEY from your shell or a .env file in this directory
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    restart: unless-stopped
